# NekoFM React/Tauri note
#
# This file is intentionally safe to commit. Do not put real server URLs,
# usernames, passwords, music paths, or tokens here.
#
# Current status:
# - The old Flutter app was removed on 2026-06-05.
# - The React/Tauri app in this folder has the main screens, playback,
#   downloads, liked songs, playlists, local metadata, offline fallbacks,
#   queue controls, reorder, repeat/shuffle, and Settings basics ported.
# - It is macOS desktop-first right now.
#
# Still needed before calling the app fully done:
# - Manual checks for offline covers, recheck cover repair, downloads/delete
#   cleanup, playback edge cases, SQLite persistence, credentials behavior,
#   packaging permissions, and export.
#
# Read the full handoff before continuing work:
# ../docs/react-tauri-migration-status.md
